server:
  port: 8080
spring:
  application:
    name: spring-cloud-eureka-client2
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
